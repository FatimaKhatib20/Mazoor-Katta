import'package:flutter/material.dart';

class doneJ extends StatefulWidget {
  const doneJ({Key? key}) : super(key: key);

  @override
  State<doneJ> createState() => _doneJState();
}

class _doneJState extends State<doneJ> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      appBar: null,
    );
  }
}
